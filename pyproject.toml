[project]
name = "food-delivery-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.31.0",
    "fastapi[standard]>=0.141.1",
    "jinja2>=3.1.6",
    "pydantic-settings>=2.15.0",
    "sqlmodel>=0.0.39",
]

[tool.uv]
# Project lives inside OneDrive, which uses cloud-file placeholders that
# can't be hardlinked to uv's cache (outside OneDrive). Copy instead.
link-mode = "copy"
